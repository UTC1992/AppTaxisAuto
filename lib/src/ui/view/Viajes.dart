import 'package:flutter/material.dart';

class Viajes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Todavía no esta lista :D '),
      ),
    );
  }
}